# Server
server:
  port: 3000

spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: cfgpassword
      username: cfguser
      retry:
        max-attempts: 10
        max-interval: 15000
        initial-interval: 10000
