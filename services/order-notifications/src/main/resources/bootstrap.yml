server:
  port: 0

spring:
  application:
    name: order-notifications
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
      password: cfgpassword
      username: cfguser
      retry:
        max-attempts: 10
        max-interval: 15000
        initial-interval: 10000
